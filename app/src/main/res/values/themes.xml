<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Androidarchitecturetemplates" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash theme; postSplashScreenTheme swaps back to the real theme. -->
    <style name="Theme.Androidarchitecturetemplates.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_note</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.Androidarchitecturetemplates</item>
    </style>
</resources>
